<section class="hero has-background-grey-lighter">
    <div class="hero-body">
        <div class="container">
            <div class="columns is-centered">
                <div class="column">
                    <form action="" class="box">
                        <label class="label is-large" for="" class="label">Edit
                            Blog Post</label>
                        <div class="field">
                            <label for="" class="label">Subject / Title</label>
                            <div class="control">
                                <input
                                    type="text"
                                    placeholder="10 Ways on how to create Blog Posts"
                                    class="input subject-input"
                                    value="{{postData.title}}" required/>
                            </div>
                        </div>
                        <div class="field-body">
                            <div class="field">
                                <label for="" class="label">Content</label>
                                <div class="control">
                                    <textarea
                                        class="textarea content-input"
                                        placeholder="Practicing your coding skills is a rewarding way to understand deeper the process of building a website. Reading about software resources helps you find more interesting way to create a unique designs or frameworks..."
                                        required
                                    >{{postData.content}}</textarea>
                                </div>
                            </div>
                        </div>
                        <div class="field is-grouped is-grouped-right mt-3">
                            <button class="button is-success edit-submit">
                                Submit
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
    <div class="current-post-id is-invisible">{{postData.id}}</div>
</section>
<script src="/js/edit.js"></script>
